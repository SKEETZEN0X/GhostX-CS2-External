#include "configs.hpp"

namespace Engine {
    void configs::save(const char* name) {
        // standalone menu config logic disabled
    }

    void configs::load(const char* name) {
        // standalone menu config logic disabled
    }
}